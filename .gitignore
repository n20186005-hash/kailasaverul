# Dependencies
node_modules

# Build output (regenerated by `astro build`, deployed via wrangler)
dist

# Astro cache / types
.astro

# Cloudflare Workers local state & logs
.wrangler

# Optional pnpm workspace file created on install (single-package project)
pnpm-workspace.yaml

# OS / editor
.DS_Store
Thumbs.db
*.log
