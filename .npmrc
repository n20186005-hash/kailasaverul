# Cloudflare Pages (pnpm 12) blocks dependency build scripts by default.
# esbuild (used by Astro/Vite) needs its post-install to fetch its binary.
dangerouslyAllowAllBuilds=true
enable-pre-post-scripts=true
